angular.module('justWrite.editor')

        .factory('scrapServices', ['$http', '_',
            function ($http, _) {

            
            var services = {
                
            };

            return services;
        }]);
