angular.module('justWrite.editor')

        .factory('commitServices', ['$http', '_',
            function ($http, _) {

            
            var services = {
                
            };

            return services;
        }]);
